//! KiCad `.net` S-expression emitter. Placeholder for the netlist PR.
//!
//! Uses the AST builder from `kicad-parser`'s `sexpr_builder` module +
//! `kicad-writer`'s `sexpr_render` — see
//! `reference_kicad_sexpr_ast_pipeline` memory note for the convention.
