//! Schematic element rendering — wires, symbols, labels, pins, etc.
//!
//! Each submodule handles rendering one schematic element type using
//! the Iced Canvas `Frame` API.

// Rendering submodules will be added as we implement each element type.
// Phase 3 (v0.4.0) will populate these.
